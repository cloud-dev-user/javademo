pipeline{ 

    agent any
  stages {
     stage('checkout') {
        steps {
             git 'https://github.com/cloud-dev-user/javademo.git'
               }
             }

     stage('test') {
        steps {
           echo '"testing is in progress"'
            }
         }
}
}
